{
  "name": "carecredits",
  "version": "1.0.0",
  "description": "On-Chain Caregiver Funding & Compliance on Stellar",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:frontend": "node --test tests/**/*.test.js",
    "test:backend": "npm --prefix backend test",
    "test": "node --test tests/**/*.test.js && npm --prefix backend test"
  },
  "dependencies": {
    "@stellar/freighter-api": "^2.0.0",
    "@stellar/stellar-sdk": "^14.0.0",
    "vite": "^5.4.11"
  }
}
